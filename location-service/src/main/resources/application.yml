spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: crud
    password: '%i0WS%GZFKpxI2'
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
